import elechons.regress_temp as r

r.init('mean')

print('note this script puts the plots 21 and 22 in plts/autoregression_fit_imgs/ and also prints the results of Fig. 23')

r.plot_precision_v_dist()